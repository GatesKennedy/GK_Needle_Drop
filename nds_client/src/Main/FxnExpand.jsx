import React from 'react';
import PropTypes from 'prop-types';

const FxnExpand = ({ head, body }) => {
  return <div></div>;
};

FxnExpand.propTypes = {
  head: PropTypes.object.isRequired,
  body: PropTypes.object.isRequired
};

export default FxnExpand;
